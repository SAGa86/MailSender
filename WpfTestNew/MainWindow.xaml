<Window x:Class="WpfTestNew.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfTestNew"
        xmlns:vm="clr-namespace:WpfTestNew.ViewModels"
        mc:Ignorable="d"
        Title="{Binding Title}" Height="450" Width="800">
    <Window.DataContext>
        <vm:MainWindowViewModel />
    </Window.DataContext>
    <Grid>
        <Slider Name="Translator" Margin="5" VerticalAlignment="Top" Minimum="0" Maximum="600"/>
        <Ellipse Width="50" Height="50" Fill="Blue" Margin="32,55,718,329">
            <Ellipse.RenderTransform>
                <TranslateTransform X ="{Binding Value, ElementName=Translator}"/>
            </Ellipse.RenderTransform>
        </Ellipse>
        <TextBox Text="{Binding Title, UpdateSourceTrigger=PropertyChanged}" VerticalAlignment="Top" HorizontalAlignment="Left" Margin="15,30" Width="160"/>

    </Grid>
</Window>
